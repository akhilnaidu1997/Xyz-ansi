- name: prompting for vars
  hosts: frontend
  become: yes
  vars_prompt:
  - name: username
    prompt: please enter your name here
    private: false
  tasks:
  - name: prompting for name
    ansible.builtin.debug:
      msg: "Hello {{ username }}, how are you doing?"