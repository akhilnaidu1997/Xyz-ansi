- name: using loop 
  hosts: frontend
  become: yes
  tasks:
  - name: loops
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: nginx, state: present }
    - { name: zip, state: present }
    - { name: git, state: absent }